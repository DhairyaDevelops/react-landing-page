# Fieldnote Studio application

Original React interface and styles with pure validation and deterministic brief generation. All form interactions stay in browser memory. No messages are sent or stored.
